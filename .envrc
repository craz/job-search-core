#!/usr/bin/env bash
# direnv evaluates this file on directory entry. The helper creates/synchronizes
# the project environment, while PATH_add avoids requiring manual activation.
source_env_if_exists .env.local
./scripts/ensure-venv.sh
export VIRTUAL_ENV="$PWD/.venv"
PATH_add .venv/bin

